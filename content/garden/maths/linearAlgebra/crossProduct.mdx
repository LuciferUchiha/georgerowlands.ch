# Cross Product

only defined in 3d space, gives a vector that is orthogonal to the two vectors being multiplied.

```math
\boldsymbol{x} \times \boldsymbol{y} = \|x\| \|y\| \sin(\theta) \boldsymbol{n}
```