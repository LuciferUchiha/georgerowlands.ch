# Another Page

```js filename="demo.js"
let a = 1;
if(a == b && c != d)
console.log(a);
```

## Component

import {useState} from 'react'

export const InlineCounter = () => {
    const [count, setCount] = useState(0);
    return (
        <div>
            <button onClick={() => setCount(count + 1)}
                    className="bg-primary-700 hover:bg-primary-800 font-bold text-white py-2 px-4 mt-8 border border-primary-700 rounded">
                Clicked {count} times
            </button>
        </div>
    );
};

<InlineCounter/>

## External Component

import Counter from '../../components/Counter/counter'


<Counter/>

## PlantUML

{/* TODO This needs to be changed to use the theme in this project when the repo is made public */}

```plantuml
@startuml
!theme purplerain from https://raw.githubusercontent.com/LuciferUchiha/nextra-garden/main
interface Command {
    execute()
    undo()
}
class Invoker{
    setCommand()
}
class Client
class Receiver{
    action()
}
class ConcreteCommand{
    execute()
    undo()
}
Command <|-down- ConcreteCommand
Client -right-> Receiver
Client --> ConcreteCommand
Invoker o-right-> Command
Receiver <-left- ConcreteCommand
@enduml
```

## Callout

import Callout from "../../components/Callout/callout"

<Callout>
    Today is Friday.
</Callout>

<Callout type="info">
    Today is Friday.
</Callout>

<Callout type="warning">
    **Do not do this** Today is Friday.
</Callout>

<Callout type="error">
    Today is Friday.
</Callout>

<Callout type="example">
    This is an example of how to write java code:

    ```java
    System.out.println("Hello World!");
    ```
</Callout>

<Callout type="todo">
    **This is still under construction!**

    Today is Friday.
</Callout>

<Callout emoji="🐕">
    Today is Friday.
</Callout>
