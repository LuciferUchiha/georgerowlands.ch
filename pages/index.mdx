import { ReactTyped } from "react-typed";
import Graph from "~/components/Graph/Graph";

<div className="h-screen flex flex-row w-full" >
  <div className="self-center border border-[#ECECEC] dark:border-[#2A2A2A]">
    <Graph className="hidden"/>
  </div>
  <div className="mt-64 ml-8">
      <ReactTyped 
          strings={["Hello there, my name is"]}
          className="text-3xl"
          typeSpeed={40}
          showCursor={false}
          startDelay={500}
      />

      <br />

      <ReactTyped
          strings={["George Rowlands"]}
          className="font-bold text-primary-500 dark:text-primary-600 text-6xl"
          typeSpeed={40}
          startDelay={2200}
          showCursor={false}
      />

  </div>
</div>
